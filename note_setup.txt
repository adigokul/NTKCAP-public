set root
cd to NTK file
check for env name

for editor window
git config --global core.editor "code --wait"
definition of merge
if only one person make the change, he is the boss
git checkout -b autoinstall
